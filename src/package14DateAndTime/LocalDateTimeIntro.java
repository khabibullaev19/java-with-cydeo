package package14DateAndTime;

public class LocalDateTimeIntro {

    public static void main(String[] args) {

        // A class that provides the ability to use years, months, days, hours, minutes, seconds,
        // and nanoseconds to represent times numerically.

        // We can use one of the following static methods of the LocalDateTime class to get the LocalDateTime object:
            // now() -> returns LocalDateTime object with the current date and time
            // of() -> returns LocalDateTime object with specified years, months, days, hours, minutes, seconds, and nanoseconds.

        // LocalDateTime class can use all the methods of LocalTime and LocalDate classes.

    }

}
