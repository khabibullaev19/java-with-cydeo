package package14DateAndTime;

public class LocalTimeIntro {

    public static void main(String[] args) {

        // A class that provides the ability to use hours, minutes, seconds, and nanoseconds
        // to represent times numerically.

        // We can use one of the following static methods of the LocalTime class to get the LocalTime object:
            // now() -> returns LocalTime object with current time
            // of() -> returns LocalTime object with specified hours, minutes, seconds, and nanoseconds(in 24 hours format).

        // getHour() ->
        // getMinute() ->
        // getSecond() ->
        // getNano() ->
        // plusHours() ->
        // plusMinutes() ->
        // plusSeconds() ->
        // plusNanos() ->
        // minusHours() ->
        // minusMinutes() ->
        // minusSeconds() ->
        // minusNanos() ->
        // isEquals() ->
        // isBefore() ->
        // isAfter() ->

    }

}
