package package16oop.inheritance;

public class SuperKeyword {

    public static void main(String[] args) {

        // The "super" keyword refers to an object's super class(Parent).
        // We can use the super keyword super() to call a super class constructor.

        // If the parent class has a default constructor, the child class calls it
        // implicitly. Otherwise, the parent class's constructor needs to be called
        // explicitly in the child class.

    }

}
