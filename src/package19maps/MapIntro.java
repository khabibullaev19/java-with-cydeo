package package19maps;

public class MapIntro {

    /*
          -> Map > HashMap - LinkedHashMap - HashTable - SortedMap > TreeMap

        - Collection of pairs.
        - Key = Value.

        - put() = add element
        - replace() = replacing value (give key, new value)
        - containsValue() = checks if value exists (give value)
        - clear() = removes all pairs from a map
        - values() = returning all values of a map as a collection
        - getValue() = use with entrySet()
        - get() = getting a value(give key)
        - remove() = removes pair, provide key
        - isEmpty() = checks if map is empty
        - putAll() = adding multiple pairs(to a map, another map), give a map
        - entrySet() = returning all entries(pairs)
        - setValue() = use with entrySet()
        - size() = getting total number of pairs
        - containsKey() = checks if key exists (give key)
        - equals() = checking two maps if equal
        - keySet() = getting all keys of a map
        - getKey() = use with entrySet()

     */

}
